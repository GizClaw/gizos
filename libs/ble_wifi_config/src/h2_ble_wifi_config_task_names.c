#include "h2_ble_wifi_config_task_names.h"

/* Keep the runtime task identity linked to the public policy constant. */
const char h2_ble_wifi_config_worker_task_name[] =
    H2_BLE_WIFI_CONFIG_WORKER_TASK_NAME_VALUE;
