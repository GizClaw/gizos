# SZP Audio System <Badge type="warning" text="WIP" />

## Partition layout

待补充 `audio-system` 的 partition table。

## sdkconfig

待补充 `audio-system` 的 `sdkconfig` defaults 和关键配置。

## 预期表现

待补充 Audio System app 正常运行时的音频行为、屏幕内容和串口输出。
