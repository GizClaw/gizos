# SZP Display <Badge type="warning" text="WIP" />

## Partition layout

待补充 `display` 的 partition table。

## sdkconfig

待补充 `display` 的 `sdkconfig` defaults 和关键配置。

## 预期表现

待补充 Display app 正常运行时屏幕应显示的内容。
