# AMOLED GizClaw Ping Speed <Badge type="warning" text="WIP" />

## Partition layout

待补充 `gizclaw-ping-speed` 的 partition table。

## sdkconfig

待补充 `gizclaw-ping-speed` 的 `sdkconfig` defaults 和关键配置。

## 预期表现

待补充 GizClaw Ping Speed app 正常运行时屏幕和串口应显示的内容。
