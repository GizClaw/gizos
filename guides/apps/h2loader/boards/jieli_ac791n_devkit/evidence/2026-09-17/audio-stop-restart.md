# Audio provider stop/restart cycles — 2026-09-17

## Outcome and source

The AC791N DevKit board audio provider survived 10 and then 50 controlled stop → restart cycles of speaker playback and microphone capture with no leak, no stuck writer or drainer and no dropped callback: both runs of the new `audio-stop-restart` diagnostic (Issue #450) printed `result=ok` on every cycle line and on the summary line, heap free stayed at exactly `7171688` bytes from the end of cycle 1 to the end of the last cycle, the SDK task count stayed at `16` on every cycle, every stop completed through the normal `h2_smoke_audio_system_stop()` path on the first attempt, and the provider idle probe reported no open track, no retained operation, no PCM ring storage, no SDK server handle, microphone closed and speaker stopped after every stop. Tested source is `origin/main` `4fd6e947` plus the branch `jieli/audio-stop-restart` at `031b5da4` (provider idle probe, smoke stream counters, cycle diagnostic, host fixtures); the board is UID `d879349abc9f` on `/dev/cu.usbserial-20131240` at 460800, and every install went through the UART Loader into Partition 2 with no USB download, format or manual power cycle.

The diagnostic package with the default N=10 is SHA-256 `f80f53bf53e1e157dffb905df8512f304fcb00dcb46219d4358ddfa4fcecb22b` (image `34cc04d8f0d98d946d8ea111142d4ef3ed6662ec75d04e250442723766e62cbf`), and the N=50 package built with `--//projects/example/targets/h2loader_tar_zlib/audio-system/jieli_ac791n_devkit:stop_restart_cycles=50` is SHA-256 `a73eda3a891723be956ab062dbfb280969885ead2540c5d8ca8642367f08ec38` (image `ef3d350a53d9568677f3c580a7f61313c00488c5aff6139bdea94e56014ef828`); both were built natively in the `embed-zig-noble-amd64` OrbStack machine with `--config=ac791n --symlink_prefix=bazel-amd64-`, alongside the unchanged `audio_system_package`, and the generated `h2_audio_stop_restart_config.h` carried `10u` and `50u` respectively.

## Ordered results

Before the round the independent status showed the 2026-09-17 Loader `ed7d71a667fe5ef20d09afff11f4909ba33a13af7e852ec4f0ac981541290fdf` in Partition 1 with `running_partition=1`, `stage_valid=0`, `last_result=0` and the previous App `5874b4190a6d1be753bc81b503a25fd170794bc48e631fa5adf7118f8f8cc071` in Partition 2.

| Step | Result | Facts |
| --- | --- | --- |
| 1: N=10 install | PASS | `send --file` rc=0 in 35 s with checksum `f80f53bf…`; `reboot upgrade --monitor` captured `JIELI_TRIAL_TIMER state=armed id=11 delay_ms=120000`, `H2_JIELI_AUDIO_CYCLE baseline heap_free=7315688 tasks=15 cycles=10`, `H2_JIELI_AUDIO_CYCLE_READY cycles=10 result=0` at 1.8 s, `JIELI_TARGET_APP result=0`, `JIELI_APP_CONFIRM result=OK code=0 target=0 transport=0` and `JIELI_TRIAL_TIMER state=deleted id=11`. |
| 2: N=10 cycles | PASS | Ten lines `cycle=1/10` … `cycle=10/10`, all `run=0 stop=0 idle=1 result=ok`, `stop_ms=81` on every cycle, `mic_frames` 108–154, `music_frames` 18–161, `consumed` 108796–168632 bytes, `heap_free=7171688` and `tasks=16` on every cycle; summary `H2_JIELI_AUDIO_CYCLE_SUMMARY cycles=10 ok=10 failed=0 heap_baseline=7315688 heap_ref=7171688 heap_last=7171688 heap_min=7171688 tasks_baseline=15 tasks_ref=16 tasks_max=16 result=ok` at 42.7 s; the monitor also carried 20 `H2_SMOKE_AUDIO_MIC peak=…` reports. |
| 3: N=10 status | PASS | Independent status after the run: `active_role=app`, `running_partition=2`, `next_partition=2`, `stage_valid=0`, `last_result=0`, `partition_2_package_checksum=f80f53bf…`, `partition_2_image_checksum=34cc04d8…`; `reboot loader` returned `running_partition=1`, `stage_valid=0`, `last_result=0`. |
| 4: N=50 install | PASS | `send --file` rc=0 in 43 s with checksum `a73eda3a…`; the monitored upgrade boot captured the same baseline `heap_free=7315688 tasks=15 cycles=50`, `H2_JIELI_AUDIO_CYCLE_READY cycles=50 result=0` at 1.8 s, `JIELI_APP_CONFIRM result=OK code=0 target=0 transport=0` and `JIELI_TRIAL_TIMER state=deleted id=11`. |
| 5: N=50 cycles | PASS | Fifty lines `cycle=1/50` … `cycle=50/50`, all `run=0 stop=0 idle=1 result=ok`, `stop_ms` 81–961 (81 on 34 cycles, above 400 on 13 cycles, maximum 961 on cycle 32), `mic_frames` 44–154, `music_frames` 5–162, `consumed` 100796–196472 bytes, `heap_free=7171688` and `tasks=16` on every cycle; summary `H2_JIELI_AUDIO_CYCLE_SUMMARY cycles=50 ok=50 failed=0 heap_baseline=7315688 heap_ref=7171688 heap_last=7171688 heap_min=7171688 tasks_baseline=15 tasks_ref=16 tasks_max=16 result=ok` at 210.4 s. |
| 6: final status | PASS | After `reboot loader` the independent status reported `active_role=loader`, `running_partition=1`, `next_partition=1`, `stage_valid=0`, `last_result=0`, `partition_1_image_checksum=ed7d71a6…` and `partition_2_image_checksum=ef3d350a…`. |

The 144000-byte drop from `heap_baseline=7315688` to `heap_ref=7171688` and the one extra task from `tasks_baseline=15` to `tasks_ref=16` occur once, during cycle 1, and never grow afterwards; they are the SDK audio server's first-open allocations and its persistent server task, which is why the diagnostic takes its references at the end of cycle 1 while still printing the baseline.

Host evidence on the same revision: `//tools/bazel:jieli_audio_lifecycle_test` gained `cycles` (50 fake-SDK open/close cycles with balanced malloc/free, zero live servers and an all-zero idle probe), `cycle_blocked_write` (a `UINT32_MAX` write released with `H2_AUDIO_ERR_INVALID_STATE` by the normal stop path, then a clean restart) and `cycle_stale_callback` (retired encoder and decoder tokens rejected after restart while the new session streams); all three fail to compile against `main`'s provider and pass with Clang, GCC 13.3 in `embed-zig-noble-amd64` and Clang TSan, and `//tools/bazel:jieli_target_runtime_lifetime_test` covers the cycle entry's worker handshake, Runtime release, bounded-cleanup retention and single-stop retry.

## Boundaries

This round proves only stop/restart of the whole audio-system scene through `h2_smoke_audio_system_stop()` and `h2_smoke_audio_system_run()` with a 3 s stream per cycle on one board; it does not cover stop while a PAL writer is blocked on hardware (host-only), drain timing at the DAC, injected SDK failures, concurrent volume changes, BLE or Wi-Fi activity during cycling, or power-loss behaviour, and the audible output was not judged by ear. The `stop_ms` spread up to 961 ms in the N=50 run comes from the SDK decoder STOP/close requests, not from the provider gate, and stays far below the 1000 ms writer deadline; it is recorded, not explained.
