diff --git a/apps/demo/demo_ble/bt_ble/ble.c b/apps/demo/demo_ble/bt_ble/ble.c
--- a/apps/demo/demo_ble/bt_ble/ble.c
+++ b/apps/demo/demo_ble/bt_ble/ble.c
@@ -24,5 +24,8 @@ void bt_ble_module_init(void)
     extern int le_controller_set_mac(void *addr);
     lib_make_ble_address(tmp_ble_addr, (u8 *)bt_get_mac_addr());
+    /* Give this diagnostic image a fresh controller identity so macOS cannot
+     * reuse pairing/GATT state accumulated by earlier experimental images. */
+    tmp_ble_addr[0] ^= 0x04;
     le_controller_set_mac((void *)tmp_ble_addr);
     printf("\n-----edr + ble 's address-----");
     put_buf((void *)bt_get_mac_addr(), 6);
