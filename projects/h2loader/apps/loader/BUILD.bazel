package(default_visibility = ["//visibility:public"])

filegroup(
    name = "loader",
    srcs = glob(
        ["**"],
        allow_empty = True,
        exclude = ["BUILD.bazel"],
    ) + [
        "//projects/h2loader/apps/loader/app:loader",
    ],
)
