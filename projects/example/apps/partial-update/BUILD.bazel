package(default_visibility = ["//visibility:public"])

filegroup(
    name = "partial_update_smoke",
    srcs = glob(
        ["**"],
        allow_empty = True,
        exclude = [
            "BUILD.bazel",
            "README.md",
        ],
    ) + [
        "//projects/example/apps/partial-update/app:partial_update_smoke",
    ],
)
