package(default_visibility = ["//visibility:public"])

filegroup(
    name = "dinobounce",
    srcs = glob(
        ["**"],
        allow_empty = True,
        exclude = [
            "BUILD.bazel",
            "README.md",
        ],
    ) + [
        "//projects/pixa_games/apps/dinobounce/app:dinobounce",
    ],
)
