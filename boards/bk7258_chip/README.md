# BK7258 Chip Board

Repository-owned bare BK7258 split-chip board. The `compile_only` layout owns
the AP/CP configuration, GPIO inputs, memory regions, and partition metadata so
compile validation does not inherit an SDK demo-board configuration.
