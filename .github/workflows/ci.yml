name: CI

on:
  pull_request:
    branches: [main]
    types: [opened, synchronize]
  push:
    branches: [main]
  workflow_dispatch:
    inputs:
      checkout_sha:
        description: Exact commit to build; defaults to the selected ref
        required: false
        type: string

permissions:
  actions: read
  contents: read

concurrency:
  group: ${{ github.workflow }}-${{ inputs.checkout_sha || github.ref }}
  cancel-in-progress: true

env:
  BAZEL_VERSION: "9.2.0"
  BAZEL_LINUX_X86_64_SHA256: 7668a95db1250f12c40407251e4e203b4ec8bf39bc495d2f485b2d8c99048694
  BAZEL_MACOS_ARM64_SHA256: dd466352a3e4d3581b8898740ee1ff208866ccbe25f8d367c5dcb950219587e6
  BAZEL_WINDOWS_X86_64_SHA256: 5fc2f2805b8c697a54732558576938d06bab63aa0f9b6610cc01d2cae0388705

jobs:
  private-dependencies:
    if: >-
      github.event_name != 'pull_request' ||
      github.event.pull_request.head.repo.full_name == github.repository
    name: Private dependency access
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - id: app-token
        name: Create read-only h2vivi token
        uses: actions/create-github-app-token@fee1f7d63c2ff003460e3d139729b119787bc349 # v2
        with:
          app-id: ${{ secrets.H2VIVI_GITHUB_APP_ID }}
          private-key: ${{ secrets.H2VIVI_GITHUB_APP_PRIVATE_KEY }}
          owner: h2vivi

      - name: Check out Firmwares devenv manifest
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2
        with:
          repository: h2vivi/firmware-devenv
          path: firmware-devenv
          fetch-depth: 1
          persist-credentials: false
          submodules: false
          token: ${{ steps.app-token.outputs.token }}

      - name: Verify private SDK repository access
        env:
          GH_TOKEN: ${{ steps.app-token.outputs.token }}
        shell: bash
        run: |
          test -f firmware-devenv/export.sh
          for repository in \
            firmware-devenv \
            bk-avdk-smp \
            bk3633 \
            AC695N_Soundbox_SDK \
            fw-AC791N_SDK; do
            test "$(gh api "repos/h2vivi/$repository" --jq .full_name)" = "h2vivi/$repository"
          done

  host:
    name: ${{ matrix.name }}
    runs-on: ${{ matrix.runner }}
    timeout-minutes: 30
    permissions:
      actions: read
      contents: read
      id-token: write
    strategy:
      fail-fast: false
      matrix:
        include:
          - name: Linux x86_64 / Build + Test
            runner: ubuntu-24.04
            config: linux_x86_64
            task: build-test
            asset: linux-x86_64
            sha: 7668a95db1250f12c40407251e4e203b4ec8bf39bc495d2f485b2d8c99048694
          - name: macOS arm64 / Build + Test
            runner: macos-15
            config: macos_arm64
            task: build-test
            asset: darwin-arm64
            sha: dd466352a3e4d3581b8898740ee1ff208866ccbe25f8d367c5dcb950219587e6
    steps:
      - name: Check out GizOS
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2
        with:
          fetch-depth: 1
          persist-credentials: false
          ref: ${{ inputs.checkout_sha || github.event.pull_request.head.sha || github.sha }}
          submodules: recursive

      - name: Install Linux SDL3 build dependencies
        if: runner.os == 'Linux'
        timeout-minutes: 5
        shell: bash
        run: |
          apt_network_options=(
            -o Acquire::Retries=0
            -o Acquire::http::Timeout=30
            -o Acquire::https::Timeout=30
          )
          sudo apt-get "${apt_network_options[@]}" update
          sudo apt-get "${apt_network_options[@]}" install --yes \
            cmake libx11-dev libxext-dev libxi-dev libxrandr-dev \
            ninja-build pkg-config

      - name: Install macOS SDL3 build dependencies
        if: runner.os == 'macOS'
        shell: bash
        run: |
          brew install cmake ninja pkg-config
          sudo xcode-select --switch /Applications/Xcode_16.4.app/Contents/Developer
          test "$(xcodebuild -version | head -n 1)" = "Xcode 16.4"

      - name: Validate Bazel remote cache variables
        env:
          CACHE_PROJECT: ${{ vars.BAZEL_CACHE_PROJECT_ID }}
          CACHE_URL: ${{ vars.BAZEL_REMOTE_CACHE_URL }}
          CACHE_PROVIDER: ${{ vars.GCP_WORKLOAD_IDENTITY_PROVIDER }}
          CACHE_WRITER: ${{ vars.BAZEL_CACHE_WRITER_SERVICE_ACCOUNT }}
        shell: bash
        run: |
          configured=0
          for value in "$CACHE_PROJECT" "$CACHE_URL" "$CACHE_PROVIDER" "$CACHE_WRITER"; do
            if [ -n "$value" ]; then configured=$((configured + 1)); fi
          done
          if [ "$configured" -eq 0 ]; then
            echo 'Bazel remote cache is not provisioned; using the job-local disk cache.'
          elif [ "$configured" -ne 4 ]; then
            echo 'Bazel remote cache repository variables are incomplete.' >&2
            exit 1
          elif [[ ! "$CACHE_URL" =~ ^https://storage\.googleapis\.com/[^/]+/gizos/?$ ]]; then
            echo 'BAZEL_REMOTE_CACHE_URL must be https://storage.googleapis.com/<bucket>/gizos.' >&2
            exit 1
          fi

      - name: Authenticate Bazel remote cache
        if: >-
          vars.BAZEL_REMOTE_CACHE_URL != '' &&
          (github.event_name != 'pull_request' ||
           github.event.pull_request.head.repo.full_name == github.repository)
        uses: google-github-actions/auth@7c6bc770dae815cd3e89ee6cdf493a5fab2cc093 # v3
        with:
          project_id: ${{ vars.BAZEL_CACHE_PROJECT_ID }}
          workload_identity_provider: ${{ vars.GCP_WORKLOAD_IDENTITY_PROVIDER }}
          service_account: ${{ vars.BAZEL_CACHE_WRITER_SERVICE_ACCOUNT }}
          create_credentials_file: true

      - name: Restore Bazel repository cache
        id: bazel-repository-cache
        continue-on-error: true
        uses: actions/cache/restore@0057852bfaa89a56745cba8c7296529d2fc39830 # v4
        with:
          path: ~/.cache/bazel/repository
          key: bazel-repository-v2-${{ runner.os }}-${{ runner.arch }}-${{ env.BAZEL_VERSION }}-${{ hashFiles('MODULE.bazel', 'MODULE.bazel.lock') }}

      - name: Install and verify Bazel
        shell: bash
        run: |
          bazel_path="$RUNNER_TEMP/bazel"
          curl --fail --location --silent --show-error \
            "https://github.com/bazelbuild/bazel/releases/download/$BAZEL_VERSION/bazel-$BAZEL_VERSION-${{ matrix.asset }}" \
            --output "$bazel_path"
          test "$(shasum -a 256 "$bazel_path" | awk '{print $1}')" = "${{ matrix.sha }}"
          chmod +x "$bazel_path"
          {
            printf 'BAZEL_BIN=%s\n' "$bazel_path"
            printf 'BAZEL_CONFIG=%s\n' '${{ matrix.config }}'
            printf 'BAZEL_BUILD_REMOTE_DOWNLOAD_OUTPUTS=minimal\n'
          } >> "$GITHUB_ENV"

      - name: Configure Bazel remote cache
        if: >-
          vars.BAZEL_REMOTE_CACHE_URL != '' &&
          (github.event_name != 'pull_request' ||
           github.event.pull_request.head.repo.full_name == github.repository)
        shell: bash
        run: |
          {
            printf 'BAZEL_REMOTE_CACHE_URL=%s\n' '${{ vars.BAZEL_REMOTE_CACHE_URL }}'
            printf 'BAZEL_REMOTE_CACHE_MODE=write\n'
            printf 'BAZEL_DISK_CACHE_MODE=off\n'
          } >> "$GITHUB_ENV"

      - name: Build compatible graph
        run: make bazel-build

      - name: Test compatible graph
        if: matrix.task == 'build-test'
        run: make bazel-test

      - name: Test repository policy scripts
        if: matrix.config == 'linux_x86_64'
        run: >-
          node --test
          .github/scripts/merge-eligibility-status/test.mjs
          .github/scripts/ownership-approval/test.mjs

      - name: Save Bazel repository cache
        if: >-
          always() && github.ref == 'refs/heads/main' &&
          steps.bazel-repository-cache.outputs.cache-hit != 'true'
        continue-on-error: true
        uses: actions/cache/save@0057852bfaa89a56745cba8c7296529d2fc39830 # v4
        with:
          path: ~/.cache/bazel/repository
          key: bazel-repository-v2-${{ runner.os }}-${{ runner.arch }}-${{ env.BAZEL_VERSION }}-${{ hashFiles('MODULE.bazel', 'MODULE.bazel.lock') }}

  esp32s3-compatible-graph:
    if: >-
      github.event_name != 'pull_request' ||
      github.event.pull_request.head.repo.full_name == github.repository
    name: ESP32-S3 / Build
    needs: private-dependencies
    runs-on: ubuntu-24.04
    timeout-minutes: 30
    permissions:
      actions: read
      contents: read
      id-token: write
    steps:
      - name: Check out GizOS
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2
        with:
          fetch-depth: 1
          persist-credentials: false
          ref: ${{ inputs.checkout_sha || github.event.pull_request.head.sha || github.sha }}
          submodules: recursive

      - id: app-token
        name: Create read-only h2vivi token
        uses: actions/create-github-app-token@fee1f7d63c2ff003460e3d139729b119787bc349 # v2
        with:
          app-id: ${{ secrets.H2VIVI_GITHUB_APP_ID }}
          private-key: ${{ secrets.H2VIVI_GITHUB_APP_PRIVATE_KEY }}
          owner: h2vivi

      - name: Check out Firmwares devenv
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2
        with:
          repository: h2vivi/firmware-devenv
          path: firmware-devenv
          fetch-depth: 1
          persist-credentials: false
          submodules: false
          token: ${{ steps.app-token.outputs.token }}

      - name: Check out ESP-IDF from Firmwares devenv
        shell: bash
        run: git -C firmware-devenv submodule update --init --depth 1 third_party/esp_idf_6.0

      - name: Restore ESP-IDF tool cache
        id: esp-idf-cache
        continue-on-error: true
        uses: actions/cache/restore@0057852bfaa89a56745cba8c7296529d2fc39830 # v4
        with:
          path: firmware-devenv/.tools/esp-idf
          key: esp-idf-v2-${{ runner.os }}-${{ hashFiles('tools/bazel/native_versions/esp_idf_commit.txt') }}-esp32s3

      - name: Install ESP-IDF tools
        shell: bash
        run: |
          export IDF_TOOLS_PATH="$GITHUB_WORKSPACE/firmware-devenv/.tools/esp-idf"
          firmware-devenv/third_party/esp_idf_6.0/install.sh esp32s3

      - name: Validate Bazel remote cache variables
        env:
          CACHE_PROJECT: ${{ vars.BAZEL_CACHE_PROJECT_ID }}
          CACHE_URL: ${{ vars.BAZEL_REMOTE_CACHE_URL }}
          CACHE_PROVIDER: ${{ vars.GCP_WORKLOAD_IDENTITY_PROVIDER }}
          CACHE_WRITER: ${{ vars.BAZEL_CACHE_WRITER_SERVICE_ACCOUNT }}
        shell: bash
        run: |
          configured=0
          for value in "$CACHE_PROJECT" "$CACHE_URL" "$CACHE_PROVIDER" "$CACHE_WRITER"; do
            if [ -n "$value" ]; then configured=$((configured + 1)); fi
          done
          if [ "$configured" -eq 0 ]; then
            echo 'Bazel remote cache is not provisioned; using the job-local disk cache.'
          elif [ "$configured" -ne 4 ]; then
            echo 'Bazel remote cache repository variables are incomplete.' >&2
            exit 1
          elif [[ ! "$CACHE_URL" =~ ^https://storage\.googleapis\.com/[^/]+/gizos/?$ ]]; then
            echo 'BAZEL_REMOTE_CACHE_URL must be https://storage.googleapis.com/<bucket>/gizos.' >&2
            exit 1
          fi

      - name: Authenticate Bazel remote cache
        if: vars.BAZEL_REMOTE_CACHE_URL != ''
        uses: google-github-actions/auth@7c6bc770dae815cd3e89ee6cdf493a5fab2cc093 # v3
        with:
          project_id: ${{ vars.BAZEL_CACHE_PROJECT_ID }}
          workload_identity_provider: ${{ vars.GCP_WORKLOAD_IDENTITY_PROVIDER }}
          service_account: ${{ vars.BAZEL_CACHE_WRITER_SERVICE_ACCOUNT }}
          create_credentials_file: true

      - name: Restore Bazel repository cache
        id: bazel-repository-cache
        continue-on-error: true
        uses: actions/cache/restore@0057852bfaa89a56745cba8c7296529d2fc39830 # v4
        with:
          path: ~/.cache/bazel/repository
          key: bazel-repository-v2-${{ runner.os }}-${{ runner.arch }}-${{ env.BAZEL_VERSION }}-${{ hashFiles('MODULE.bazel', 'MODULE.bazel.lock') }}

      - name: Install and verify Bazel
        shell: bash
        run: |
          bazel_path="$RUNNER_TEMP/bazel"
          curl --fail --location --silent --show-error \
            "https://github.com/bazelbuild/bazel/releases/download/$BAZEL_VERSION/bazel-$BAZEL_VERSION-linux-x86_64" \
            --output "$bazel_path"
          test "$(sha256sum "$bazel_path" | awk '{print $1}')" = "$BAZEL_LINUX_X86_64_SHA256"
          chmod +x "$bazel_path"
          printf 'BAZEL_BIN=%s\n' "$bazel_path" >> "$GITHUB_ENV"

      - name: Configure Bazel remote cache
        if: vars.BAZEL_REMOTE_CACHE_URL != ''
        shell: bash
        run: |
          {
            printf 'BAZEL_REMOTE_CACHE_URL=%s\n' '${{ vars.BAZEL_REMOTE_CACHE_URL }}'
            printf 'BAZEL_REMOTE_CACHE_MODE=write\n'
            printf 'BAZEL_DISK_CACHE_MODE=off\n'
          } >> "$GITHUB_ENV"

      - name: Build ESP32-S3 compatible graph
        shell: bash
        run: |
          . firmware-devenv/export.sh
          make bazel-build BAZEL_CONFIG=esp32s3

      - name: Save Bazel repository cache
        if: >-
          always() && github.ref == 'refs/heads/main' &&
          steps.bazel-repository-cache.outputs.cache-hit != 'true'
        continue-on-error: true
        uses: actions/cache/save@0057852bfaa89a56745cba8c7296529d2fc39830 # v4
        with:
          path: ~/.cache/bazel/repository
          key: bazel-repository-v2-${{ runner.os }}-${{ runner.arch }}-${{ env.BAZEL_VERSION }}-${{ hashFiles('MODULE.bazel', 'MODULE.bazel.lock') }}

      - name: Save ESP-IDF tool cache
        if: >-
          always() && github.ref == 'refs/heads/main' &&
          steps.esp-idf-cache.outputs.cache-hit != 'true'
        continue-on-error: true
        uses: actions/cache/save@0057852bfaa89a56745cba8c7296529d2fc39830 # v4
        with:
          path: firmware-devenv/.tools/esp-idf
          key: esp-idf-v2-${{ runner.os }}-${{ hashFiles('tools/bazel/native_versions/esp_idf_commit.txt') }}-esp32s3

  windows-compatible-graph:
    name: Windows x86_64 / Build + Test
    runs-on: windows-2025
    timeout-minutes: 30
    permissions:
      actions: read
      contents: read
      id-token: write
    steps:
      - name: Check out GizOS
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2
        with:
          fetch-depth: 1
          persist-credentials: false
          ref: ${{ inputs.checkout_sha || github.event.pull_request.head.sha || github.sha }}
          submodules: recursive

      - name: Validate Bazel remote cache variables
        shell: pwsh
        env:
          CACHE_PROJECT: ${{ vars.BAZEL_CACHE_PROJECT_ID }}
          CACHE_PROVIDER: ${{ vars.GCP_WORKLOAD_IDENTITY_PROVIDER }}
          CACHE_URL: ${{ vars.BAZEL_REMOTE_CACHE_URL }}
          CACHE_WRITER: ${{ vars.BAZEL_CACHE_WRITER_SERVICE_ACCOUNT }}
        run: |
          $configured = @(
            $env:CACHE_PROJECT,
            $env:CACHE_PROVIDER,
            $env:CACHE_URL,
            $env:CACHE_WRITER
          ).Where({ $_ }).Count
          if ($configured -eq 0) {
            Write-Host 'Bazel remote cache is not provisioned; using the job-local disk cache.'
          } elseif ($configured -ne 4) {
            throw 'Bazel remote cache repository variables are incomplete.'
          } elseif ($env:CACHE_URL -notmatch '^https://storage\.googleapis\.com/[^/]+/gizos/?$') {
            throw 'BAZEL_REMOTE_CACHE_URL must use the /gizos prefix.'
          }

      - name: Authenticate Bazel remote cache
        if: >-
          vars.BAZEL_REMOTE_CACHE_URL != '' &&
          (github.event_name != 'pull_request' ||
           github.event.pull_request.head.repo.full_name == github.repository)
        uses: google-github-actions/auth@7c6bc770dae815cd3e89ee6cdf493a5fab2cc093 # v3
        with:
          project_id: ${{ vars.BAZEL_CACHE_PROJECT_ID }}
          workload_identity_provider: ${{ vars.GCP_WORKLOAD_IDENTITY_PROVIDER }}
          service_account: ${{ vars.BAZEL_CACHE_WRITER_SERVICE_ACCOUNT }}
          create_credentials_file: true

      - name: Restore Bazel repository cache
        id: bazel-repository-cache
        continue-on-error: true
        uses: actions/cache/restore@0057852bfaa89a56745cba8c7296529d2fc39830 # v4
        with:
          path: ~/.cache/bazel/repository
          key: bazel-repository-v2-${{ runner.os }}-${{ runner.arch }}-${{ env.BAZEL_VERSION }}-${{ hashFiles('MODULE.bazel', 'MODULE.bazel.lock') }}

      - name: Install and verify Bazel
        shell: pwsh
        run: |
          $bazel = Join-Path $env:RUNNER_TEMP 'bazel.exe'
          curl.exe --fail --location --silent --show-error `
            "https://github.com/bazelbuild/bazel/releases/download/$env:BAZEL_VERSION/bazel-$env:BAZEL_VERSION-windows-x86_64.exe" `
            --output $bazel
          if ((Get-FileHash -Algorithm SHA256 $bazel).Hash.ToLowerInvariant() -ne $env:BAZEL_WINDOWS_X86_64_SHA256) {
            throw 'Bazel SHA-256 mismatch'
          }
          "BAZEL_BIN=$bazel" | Out-File -FilePath $env:GITHUB_ENV -Encoding utf8 -Append

      - name: Build and test compatible graph
        shell: pwsh
        env:
          CACHE_URL: >-
            ${{ (github.event_name != 'pull_request' ||
            github.event.pull_request.head.repo.full_name == github.repository) &&
            vars.BAZEL_REMOTE_CACHE_URL || '' }}
        run: |
          $cacheArgs = @()
          if ($env:CACHE_URL) {
            if ($env:CACHE_URL -notmatch '^https://storage\.googleapis\.com/[^/]+/gizos/?$') {
              throw 'BAZEL_REMOTE_CACHE_URL must use the /gizos prefix'
            }
            $cacheArgs = @(
              "--remote_cache=$($env:CACHE_URL.TrimEnd('/'))",
              '--google_default_credentials',
              '--remote_timeout=60',
              '--remote_retries=5',
              '--remote_upload_local_results=true',
              '--disk_cache='
            )
          }
          & $env:BAZEL_BIN @cacheArgs build --config=windows_x86_64 --lockfile_mode=off --remote_download_outputs=minimal //...
          if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }
          & $env:BAZEL_BIN @cacheArgs test --config=windows_x86_64 --lockfile_mode=off --remote_download_outputs=minimal //...
          if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }

      - name: Save Bazel repository cache
        if: >-
          always() && github.ref == 'refs/heads/main' &&
          steps.bazel-repository-cache.outputs.cache-hit != 'true'
        continue-on-error: true
        uses: actions/cache/save@0057852bfaa89a56745cba8c7296529d2fc39830 # v4
        with:
          path: ~/.cache/bazel/repository
          key: bazel-repository-v2-${{ runner.os }}-${{ runner.arch }}-${{ env.BAZEL_VERSION }}-${{ hashFiles('MODULE.bazel', 'MODULE.bazel.lock') }}

  ci-required:
    if: always()
    name: CI required
    needs:
      - private-dependencies
      - host
      - esp32s3-compatible-graph
      - windows-compatible-graph
    runs-on: ubuntu-latest
    steps:
      - name: Require every applicable execution class
        env:
          HOST_RESULT: ${{ needs.host.result }}
          ESP32S3_RESULT: ${{ needs.esp32s3-compatible-graph.result }}
          PRIVATE_DEPENDENCIES_RESULT: ${{ needs.private-dependencies.result }}
          PRIVATE_DEPENDENCIES_REQUIRED: >-
            ${{ github.event_name != 'pull_request' ||
            github.event.pull_request.head.repo.full_name == github.repository }}
          WINDOWS_RESULT: ${{ needs.windows-compatible-graph.result }}
        shell: bash
        run: |
          test "$HOST_RESULT" = success
          test "$WINDOWS_RESULT" = success
          if [ "$PRIVATE_DEPENDENCIES_REQUIRED" = true ]; then
            test "$PRIVATE_DEPENDENCIES_RESULT" = success
            test "$ESP32S3_RESULT" = success
          else
            test "$PRIVATE_DEPENDENCIES_RESULT" = skipped
            test "$ESP32S3_RESULT" = skipped
          fi
