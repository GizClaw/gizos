package(default_visibility = ["//visibility:public"])

filegroup(
    name = "repository_definitions",
    srcs = glob([
        "*.BUILD.bazel",
        "*.bzl",
    ]),
)

exports_files(glob(["patches/*.patch"]))
